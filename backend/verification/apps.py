from django.apps import AppConfig


class VerificationConfig(AppConfig):
    """Configuration for the verification app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'verification'
    verbose_name = 'Verification'
