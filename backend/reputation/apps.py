from django.apps import AppConfig


class ReputationConfig(AppConfig):
    """Configuration for reputation app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'reputation'
    verbose_name = 'Reputation'
